import 'package:flutter/material.dart';

class PostSearchBarMobile extends StatelessWidget {
  const PostSearchBarMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
